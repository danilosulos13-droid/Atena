# ATENA — stack opcional de aprendizagem autônoma / fine-tuning LoRA
# Instale depois do core: python -m pip install -r setup/requirements-autonomous-learning.txt
# CPU funciona, mas treino local de LLM é muito mais rápido com GPU compatível.
datasets>=2.18,<4
accelerate>=0.27,<2
safetensors>=0.4,<1
peft>=0.10,<1
transformers>=4.40,<6
torch>=2.2,<3
sentencepiece>=0.1.99,<1

autoflake>=2.2,<3
ruff>=0.4,<1
