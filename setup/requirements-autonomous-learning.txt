# ATENA — stack de aprendizagem autônoma / fine-tuning LoRA
# Versões exatas para evitar regressões do import lazy do Transformers.
datasets==3.6.0
transformers==4.55.4
peft==0.17.1
accelerate==1.10.1
safetensors==0.5.3
torch>=2.2,<3
sentencepiece==0.2.0

autoflake>=2.2,<3
ruff>=0.4,<1
